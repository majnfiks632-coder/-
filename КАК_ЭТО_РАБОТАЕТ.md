# Как это работает: ДубинаОС изнутри

Документ-учебник для тех, кто хочет понять, как устроена ДубинаОС от первого
байта BIOS до отладочного окна F12. Всё с нуля, без предположений о
предварительных знаниях про операционные системы.

Стиль каждого раздела одинаковый:

1. **Зачем** — что мы вообще пытаемся сделать и почему это нужно.
2. **Что в железе** — что реально происходит в процессоре / на шинах.
3. **Как в нашем коде** — конкретные файлы и функции с объяснениями.
4. **Что мог бы пойти не так** — типовые баги, которые ловят начинающих
   разработчиков ОС. Часть из них мы лично ловили — отмечено «*мы наступили*».

---

## Оглавление

- [Введение: что такое операционная система и микроядро](#введение)
- [Этап 1 — Фундамент: как процессор начинает выполнять наш код](#этап-1)
  - [1.1 Что делает BIOS](#11-биос)
  - [1.2 Real mode: 16-битный мир из 1981 года](#12-real-mode)
  - [1.3 MBR — наши первые 512 байт](#13-mbr)
  - [1.4 Stage2 и переход в Protected Mode](#14-stage2)
  - [1.5 Long Mode: 64 бита и страничная память](#15-long-mode)
  - [1.6 Multiboot2 — альтернативный путь через GRUB](#16-multiboot2)
  - [1.7 VGA-консоль, свой шрифт и кодировка](#17-vga)
  - [1.8 aarch64: совсем другой мир](#18-aarch64)
- [Этап 2 — Микроядро: память, потоки, общение](#этап-2)
  - [2.1 Физическая память: битмап страниц](#21-физическая-память)
  - [2.2 Виртуальная память: 4-уровневые таблицы](#22-виртуальная-память)
  - [2.3 Ядерная куча: динамическое выделение](#23-куча)
  - [2.4 Потоки и переключение контекста](#24-потоки)
  - [2.5 Кооперативный планировщик](#25-планировщик)
  - [2.6 ИПК: общение через порты](#26-ипк)
  - [2.7 Способности: безопасность через хэндлы](#27-способности)
- [Этап 3 — Прерывания: реакция на железо](#этап-3)
  - [3.1 GDT с TSS: фундамент защищённого режима](#31-gdt)
  - [3.2 IDT: таблица обработчиков прерываний](#32-idt)
  - [3.3 8259 PIC: контроллер прерываний из 1976](#33-pic)
  - [3.4 PIT: таймер 100 раз в секунду](#34-pit)
  - [3.5 Каркас драйверов](#35-каркас)
- [Этап 4 — Драйверы: клавиатура и отладочное окно](#этап-4)
  - [4.1 Журнал логов](#41-журнал)
  - [4.2 PS/2-клавиатура](#42-клавиатура)
  - [4.3 Отладочное окно по F12](#43-окно)
- [Что дальше](#что-дальше)

---

<a id="введение"></a>
## Введение: что такое операционная система и микроядро

**Операционная система** — это программа, которая запускается первой на
голом железе и предоставляет:

1. *Абстракции* — вместо сырых команд диску мы пишем «сохрани файл `привет.txt`».
2. *Изоляцию* — два процесса не могут случайно затоптать память друг друга.
3. *Распределение ресурсов* — кто получает CPU, сколько памяти, и т.п.

ОС бывают разной формы. Главная развилка — **монолитное ядро vs микроядро**:

```
    МОНОЛИТ (Linux, Windows NT)        МИКРОЯДРО (наш путь)
    ┌─────────────────────────┐        ┌─────────────────────┐
    │ драйвер диска           │        │ ядро (мин.)         │
    │ файловая система        │        │  - переключение     │
    │ сеть                    │        │  - память           │
    │ драйвер клавиатуры      │   vs   │  - ИПК              │
    │ память                  │        └────────┬────────────┘
    │ переключение            │                 │ сообщения
    │ всё — в едином адресном │        ┌────────┴────────────┐
    │ пространстве ядра       │        │ службы (отдельные   │
    └─────────────────────────┘        │ процессы):          │
                                       │  - драйвер диска    │
                                       │  - файл. система    │
                                       │  - сеть             │
                                       │  - клавиатура       │
                                       └─────────────────────┘
```

**Микроядро** хорошо тем, что глюк в драйвере не валит всю ОС, и расширять
систему можно «извне». Плохо тем, что общение через сообщения медленнее
прямых вызовов.

Мы выбрали микроядро потому, что:
- *Учебная цель*: проще понять, что должно быть в ядре, а что не должно.
- *Гибкость*: добавить новый драйвер можно без перекомпиляции ядра.
- *Безопасность*: каждая служба в своём адресном пространстве (это сделаем
  в следующих этапах, после Этапа 4).

В четырёх первых этапах мы построили **сам уровень ядра**. Сетки, ФС,
оболочки — это уже службы, которые подключим в следующих этапах.

Карта того, что уже работает в ДубинаОС:

```
┌──────────────────────────────────────────────────────────────┐
│                    Кольцо 0 (ядро)                           │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ Этап 1 │ Загрузчик, VGA, шрифт «Дубина», ДубКод         │ │
│ ├──────────────────────────────────────────────────────────┤ │
│ │ Этап 2 │ Память, куча, потоки, ИПК, способности         │ │
│ ├──────────────────────────────────────────────────────────┤ │
│ │ Этап 3 │ GDT/TSS, IDT, PIC, PIT, каркас драйверов       │ │
│ ├──────────────────────────────────────────────────────────┤ │
│ │ Этап 4 │ Журнал, PS/2-клавиатура, отладочное окно (F12)│ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

Дальше — пройдёмся по каждому этапу.

---

<a id="этап-1"></a>
## Этап 1 — Фундамент: как процессор начинает выполнять наш код

<a id="11-биос"></a>
### 1.1 Что делает BIOS

**Зачем.** Когда ты нажимаешь кнопку питания, процессор выходит из reset.
Что ему делать? Где наша программа? Память пуста, диск ещё не прочитан, мы
даже не знаем, какие у нас устройства.

**Что в железе.** На материнской плате есть микросхема с **BIOS** (Basic
Input/Output System) или **UEFI** — небольшая программа в энергонезависимой
памяти. Процессор начинает с её адреса (классически — `0xFFFF_FFF0`).
BIOS:

1. Запускает **POST** (power-on self-test) — проверяет память, видеокарту,
   клавиатуру.
2. Сканирует загрузочные устройства — диски, USB, CD.
3. На первом найденном устройстве читает **первый сектор** (LBA 0, 512 байт)
   в память по адресу `0x7C00`.
4. Проверяет, что последние два байта сектора — `0x55 0xAA` (магия MBR).
5. Передаёт управление на `0x7C00`.

Эти 512 байт — наша **MBR** (Master Boot Record). С этого момента BIOS уходит
в фон, и за всё отвечаем мы.

> **Что мог бы пойти не так:** забыть положить `0x55 0xAA` в конец MBR.
> BIOS просто скажет «Operating System not found» и не запустит нас.
> *Мы наступили*: первая попытка собрать MBR без `.byte 0x55, 0xAA`
> в конце — QEMU сразу написал «No bootable device».

<a id="12-real-mode"></a>
### 1.2 Real mode: 16-битный мир из 1981 года

**Зачем.** Совместимость с самыми первыми IBM PC 1981 года. Каждый x86
процессор начинает работу в **real mode** — режиме, где он эмулирует Intel
8086. 16-битные регистры, всего 1 МиБ памяти, никакой защиты.

**Что в железе.**

- Регистры: `ax`, `bx`, `cx`, `dx`, `si`, `di`, `bp`, `sp` — все 16-битные.
- Сегментные регистры: `cs`, `ds`, `es`, `ss`. Линейный адрес = `сегмент * 16 + смещение`.
- Адрес максимум 20 бит = 1 МиБ. Для совместимости с 8086.
- Никаких прерываний, кроме INT — программных вызовов BIOS.

INT 10h — видео (печать символа, очистка экрана). INT 13h — диск
(чтение сектора). INT 16h — клавиатура. И т.д. Это *call BIOS interrupts*,
без них в real mode почти ничего нельзя.

<a id="13-mbr"></a>
### 1.3 MBR — наши первые 512 байт

**Что мы делаем.** Размер MBR — 512 байт, но 64 байта в конце уходят на
таблицу разделов и магию `0x55 0xAA`. Реально под код у нас ~440 байт.
Этого мало, чтобы поместить что-то серьёзное, поэтому MBR делает
минимальную задачу: **читает с диска Stage2 в память и прыгает в него**.

**Файл:** `загрузчик/x86_legacy/mbr.s`

```asm
; Минимальный псевдокод, реальный код в файле
[BITS 16]
[ORG 0x7C00]

start:
    cli                     ; запрещаем прерывания
    xor ax, ax
    mov ds, ax              ; обнуляем сегментные регистры
    mov es, ax
    mov ss, ax
    mov sp, 0x7C00          ; стек растёт вниз от 0x7C00

    ; Печатаем "DubinaOS MBR: loading stage2..." через INT 10h
    call печать_сообщения

    ; Читаем 4 сектора (Stage2) с диска через INT 13h функцию 0x42 (LBA)
    mov si, dap             ; DAP = Disk Address Packet
    mov ah, 0x42
    mov dl, 0x80            ; первый жёсткий диск
    int 0x13
    jc ошибка               ; если CF=1 — ошибка

    jmp 0x0000:0x7E00       ; прыгаем в Stage2
```

Структура `dap` (Disk Address Packet):
```
+0:  размер пакета (16 байт)
+1:  резерв
+2:  число секторов для чтения (4)
+4:  целевой сегмент:смещение (0x0000:0x7E00)
+8:  начальный LBA (1 — Stage2 живёт сразу за MBR)
```

> **Что мог бы пойти не так:**
> 1. Забыть `cli` — прерывания таймера выстрелят посреди настройки и сломают
>    SP.
> 2. Использовать INT 13h функцию 0x02 (CHS — Cylinder/Head/Sector) вместо
>    0x42 (LBA). На больших дисках CHS не работает.
> 3. Прыгнуть в Stage2 дальним переходом без обновления `cs` — `cs`
>    останется тем, который BIOS установил.

<a id="14-stage2"></a>
### 1.4 Stage2 и переход в Protected Mode

**Зачем.** Real mode — 1 МиБ памяти максимум, нет защиты. Чтобы запустить
ядро в нормальной памяти (хотя бы 4 ГиБ), мы должны перейти в **protected
mode** (32-бит) и потом в **long mode** (64-бит).

**Что мы делаем в Stage2** (`загрузчик/x86_legacy/stage2.s`, ~1 КиБ):

1. **Включаем линию A20.** Из соображений совместимости с 8086 у IBM PC
   *обнулялся* 21-й бит адреса (A20). Чтобы получить доступ выше 1 МиБ, A20
   надо явно включить через порт 0x92 — System Control Port:
   ```asm
   in al, 0x92
   or al, 2
   out 0x92, al
   ```

2. **Читаем ядро с диска.** Ещё одним вызовом INT 13h LBA — на этот раз
   читаем 200 секторов (≈100 КиБ) в `0x10000` (это в пределах 1 МиБ, чтобы
   ещё работал real mode).

3. **Готовим GDT (Global Descriptor Table).** В protected mode каждый
   сегментный регистр (`cs`, `ds`, …) содержит **селектор** — индекс в
   GDT. GDT — массив **дескрипторов** по 8 байт, каждый описывает сегмент
   памяти: базу, лимит, права доступа. Минимальная GDT для перехода —
   три записи: null, code32, data32.

4. **Загружаем GDT в процессор:** `lgdt [gdt_descriptor]`.

5. **Включаем CR0.PE** (бит 0 регистра CR0) — переход в protected mode:
   ```asm
   mov eax, cr0
   or eax, 1
   mov cr0, eax
   ```

6. **Дальний переход** на 32-битный код, чтобы обновить `cs`:
   ```asm
   jmp 0x08:protected_mode_entry  ; 0x08 = селектор code32
   ```

7. **В protected mode (32-бит):** копируем ядро из `0x10000` в `0x100000`
   (1 МиБ — стандартное место для ядра x86) через `rep movsd`.

8. **Магия для ядра:** перед прыжком кладём в `eax` число `0xD0BA0BE5`
   («ДубаОБэ5» — наша магическая константа). Ядро по этому числу
   понимает, что его загрузил наш собственный загрузчик, а не GRUB.

<a id="15-long-mode"></a>
### 1.5 Long Mode: 64 бита и страничная память

**Зачем.** Protected mode — это 32 бита, 4 ГиБ памяти. Современные
процессоры — 64-битные, и нам нужны 64-битные регистры (`rax`, `rbx`, …)
и большие виртуальные адреса.

**Что в железе.** Чтобы зайти в long mode, нужно:

1. **Включить PAE** (Physical Address Extension) в `CR4`.
2. **Подготовить таблицы страниц** — long mode требует включённой
   страничной памяти. Самый простой вариант: identity mapping первых 4 ГиБ
   (виртуальный адрес 0x... → физический адрес 0x...).
3. **Установить адрес PML4** (root таблицы страниц) в `CR3`.
4. **Включить EFER.LME** (Long Mode Enable) — бит 8 модельного регистра
   `IA32_EFER` через `wrmsr`.
5. **Включить CR0.PG** (Paging).
6. **Дальний переход** на 64-битный код, обновляя `cs`.

**Структура страничных таблиц:**

```
Виртуальный адрес 64-бит делится на 5 частей:
┌────────────┬────────────┬────────────┬────────────┬─────────────┐
│ PML4 idx   │ PDPT idx   │ PD idx     │ PT idx     │ Смещение    │
│ (биты 47-39│ 38-30      │ 29-21      │ 20-12      │ 11-0)       │
└────────────┴────────────┴────────────┴────────────┴─────────────┘

CR3 ──→ PML4 (4 KiB, 512 записей по 8 байт)
              │
              └──→ PDPT (4 KiB, 512 записей)
                        │
                        └──→ PD (4 KiB, 512 записей)
                                  │
                                  └──→ PT (4 KiB, 512 записей)
                                            │
                                            └──→ страница 4 KiB
```

Это даёт виртуальное адресное пространство `512^4 * 4096 = 256 ТиБ`, но
старшие биты (48-63) должны быть копией бита 47 («canonical address»).

**Файл:** `ядро/src/арх/x86_64/вход.s` (`.code32` секция перед прыжком в
long mode):

```asm
; (упрощённый псевдокод)

setup_long_mode:
    ; Идентичная карта первых 4 ГиБ через 4 KiB страницы
    ; Заполняем PML4 → PDPT → PD → 2 МиБ страницы (для скорости)
    
    ; Включаем PAE
    mov eax, cr4
    or eax, 1<<5
    mov cr4, eax
    
    ; CR3 ← адрес PML4
    mov eax, page_table_l4
    mov cr3, eax
    
    ; EFER.LME = 1
    mov ecx, 0xC0000080
    rdmsr
    or eax, 1<<8
    wrmsr
    
    ; CR0.PG = 1
    mov eax, cr0
    or eax, 1<<31
    mov cr0, eax
    
    ; Дальний переход в .code64
    jmp 0x08:long_mode_entry

[BITS 64]
long_mode_entry:
    ; Теперь у нас 64-битный режим, можно вызывать Rust.
    mov rsp, kernel_stack_top
    call _yadro_start
```

> **Что мог бы пойти не так:**
> 1. Не настроить таблицы страниц до включения CR0.PG → процессор сразу
>    падает (триплфолт → reset).
> 2. Сделать не-canonical виртуальные адреса (биты 48-63 не совпадают с
>    битом 47) → #GP (General Protection Fault).
> 3. Установить флаг PAGE_PRESENT, но не указать физический адрес → #PF.

<a id="16-multiboot2"></a>
### 1.6 Multiboot2 — альтернативный путь через GRUB

**Зачем.** Свой MBR + Stage2 — это здорово для понимания, но:
- На реальной флешке надо записывать через `dd`, что неудобно.
- GRUB уже умеет грузить с CD/DVD/USB через стандартные ISO-образы.
- На UEFI BIOS просто не запустит наш MBR.

Поэтому ядро параллельно поддерживает **Multiboot2** — стандарт, по
которому GRUB загружает ядра.

**Что в железе.** GRUB сам читает диск, разбирает FAT/ISO9660, переходит в
protected mode, читает наше ядро, и прыгает в его точку входа с:
- `eax = 0x36D76289` (магия Multiboot2)
- `ebx = адрес информационной структуры` (карта памяти, имя загрузчика, и т.д.)

Чтобы GRUB нашёл наше ядро, в первых 32 КиБ ядра должен быть
**Multiboot2-заголовок** с магией `0xE8525067`.

**Файл:** `ядро/src/арх/x86_64/вход.s` (секция в начале):

```asm
.section .multiboot2_header
header_start:
    .long 0xE85250D6        ; магия Multiboot2
    .long 0                  ; ARCH=i386
    .long header_end - header_start  ; длина заголовка
    .long -(0xE85250D6 + 0 + (header_end - header_start))  ; checksum
    
    ; ... теги (entry_address, framebuffer, и т.д.)
    
    .short 0                 ; конечный тег
    .short 0
    .long 8
header_end:
```

Файл `ядро/src/мультизагрузка2.rs` парсит структуру информации от GRUB —
карту памяти и базовое имя загрузчика. На основе карты памяти физический
аллокатор узнаёт, какие диапазоны RAM реально доступны, а какие занимает
ACPI / MMIO.

<a id="17-vga"></a>
### 1.7 VGA-консоль, свой шрифт и кодировка

**Зачем.** Чтобы видеть, что происходит, нужно что-то напечатать на
экран. Самый простой путь — **VGA-текстовый режим**, доступный из коробки
без графического драйвера.

**Что в железе.**

VGA-карта в текстовом режиме отображает буфер 80×25 ячеек, начинающийся
по физическому адресу `0xB8000`. Каждая ячейка — 2 байта:

```
Байт 0: код символа (0..255)
Байт 1: атрибут (старшие 4 бита — фон, младшие 4 — цвет текста)
```

Шрифт хранится в самой VGA-карте — *character generator*. Стандартный
шрифт IBM **CP437** содержит латиницу + рамки + псевдо-графику, но
**не содержит кириллицы**. Если просто записать байт `0xC4` (наш `Ё` в
UTF-8), VGA нарисует «─» вместо «Ё».

**Решение.** Заменить шрифт VGA на наш собственный «Дубина 8×16» с
кириллицей. Это делается через порты 0x3C4 (Sequencer) и 0x3CE (Graphics
Controller):

1. Переключаем VGA в режим записи в plane 2 (где живёт шрифт).
2. По адресу `0xA0000` пишем 256 глифов × 32 байта (хотя реально нужны 16).
3. Возвращаем VGA в текстовый режим.

После этого код символа `0xC4` будет рисовать букву «Ё», как мы решили.

**Своя кодировка ДубКод.** UTF-8 для русских букв занимает 2 байта на
символ. Для VGA-текстового режима, где каждая ячейка — 1 байт, это
неудобно. Поэтому мы создали 1-байтную кодировку **ДубКод**:

```
0x00 - 0x7F   ASCII (как обычно)
0x80 - 0x9F   А..Я (русские заглавные)
0xA0          Ё
0xA1 - 0xC0   а..я (русские строчные)
0xC1          ё
0xD0 - 0xDA   рамки: ╔ ╗ ╚ ╝ ═ ║ ╠ ╣ ╦ ╩ ╬
```

**Файлы:**
- `ядро/src/арх/x86_64/видео.rs` — драйвер VGA, загрузка шрифта.
- `ядро/src/арх/x86_64/шрифт_дубина.rs` — массив `ШРИФТ: [[u8; 16]; 256]`
  с глифами.
- `ядро/src/кодировка.rs` — функция `в_дубкод(символ: char) -> u8`.

> **Что мог бы пойти не так:**
> *Мы наступили*: при записи шрифта забыли *сохранить* предыдущее
> состояние Sequencer/GC. После записи VGA остался в графическом
> режиме записи и стал рисовать мусор. Решение: до записи **читаем и
> сохраняем** текущие значения портов 0x3C4/0x3CE, и в конце восстанавливаем.

<a id="18-aarch64"></a>
### 1.8 aarch64: совсем другой мир

ARM 64-бит — совсем не x86. Никаких BIOS, никаких MBR, никаких портов.
QEMU virt-машина грузит ядро напрямую по адресу из ELF-заголовка, в
EL2 (hypervisor). Мы:

1. Через `mrs` читаем текущий уровень исключений (Exception Level).
2. Если EL2 — переходим в EL1 через `eret` (Linux/POSIX-ядра обычно работают
   на EL1).
3. **Включаем FP/SIMD** через `CPACR_EL1.FPEN = 0b11`. Без этого Rust
   падает на первом же `core::ptr::write_volatile`, потому что Rust
   использует NEON для проверок выравнивания. *Мы наступили*.
4. Настраиваем **MMU** (аналог страничной памяти на x86) — но в этом
   этапе ARM пока работает с MMU выключенным, identity mapping реализован
   только на x86_64.
5. Запускаем UART **PL011** (адрес `0x09000000` в QEMU virt-машине) —
   ARM-аналог COM1.
6. Прыгаем в Rust-функцию `_yadro_start`.

**Файл:** `ядро/src/арх/aarch64/вход.s`

> На реальном железе ARM (например, Raspberry Pi или Asus X552E) шаги
> были бы другие — там и DTB (Device Tree Blob) от U-Boot, и ACPI, и
> другие UART. QEMU virt — синтетическая машина, очень удобная для
> разработки.

---

<a id="этап-2"></a>
## Этап 2 — Микроядро: память, потоки, общение

После Этапа 1 у нас есть процессор, бегущий 64-битный код, и VGA, который
пишет буквы. Но мы не можем:
- Выделить память (`Box::new` падает).
- Запустить второй поток.
- Сделать `String::from("привет")`.

Этап 2 — построить базовое микроядро, после которого всё это работает.

<a id="21-физическая-память"></a>
### 2.1 Физическая память: битмап страниц

**Зачем.** Когда какой-то модуль ядра говорит «дай мне 4 КиБ памяти», нам
надо знать, какие 4 КиБ свободны. Самый простой способ — **битмап**: по 1
биту на каждую страницу 4 КиБ.

**Что в железе.** RAM — это адресуемое пространство байтов от 0 до
`физической_верхней_границы`. Не вся RAM нам доступна:

- 0..640 КиБ — обычная RAM, но нижние 4 КиБ часто заняты IVT (Interrupt
  Vector Table) от BIOS.
- 640 КиБ..1 МиБ — VGA, BIOS ROM, EBDA — нельзя трогать.
- 1 МиБ..2 МиБ — наше ядро. Тоже занято.
- 2 МиБ.. — обычная RAM, мы её можем использовать.

GRUB через Multiboot2 даёт нам **карту памяти** — массив структур
«база/длина/тип». Тип `1` = доступна; тип `2` = занята ROM; и т.д.

**Файлы:**
- `ядро/src/память/физическая.rs` — `АЛЛОКАТОР` (Mutex над структурой `Битмап`).

```rust
pub struct Битмап {
    биты: &'static mut [u8],   // 1 бит на страницу 4 КиБ
    всего_страниц: usize,
    свободных: usize,
}

impl Битмап {
    pub fn выделить(&mut self) -> Option<u64> { /* ищем нулевой бит */ }
    pub fn освободить(&mut self, физ_адрес: u64) { /* ставим бит в 0 */ }
}
```

Сам массив битов хранится после ядра (примерно с адреса 2 МиБ). Алгоритм
выделения — **первое подходящее**: проходим по битам, находим первый
нулевой, помечаем единицей, возвращаем адрес.

**Как использовать:**

```rust
let физ_адрес: u64 = АЛЛОКАТОР.lock().выделить().unwrap();
// делаем что-то полезное
АЛЛОКАТОР.lock().освободить(физ_адрес);
```

> **Что мог бы пойти не так:**
> 1. Использовать страницы 0..2 МиБ как «свободные» — они на самом деле
>    заняты ядром / BIOS.
> 2. Не очищать память после выделения — там будут старые данные от
>    предыдущего владельца.

<a id="22-виртуальная-память"></a>
### 2.2 Виртуальная память: 4-уровневые таблицы

**Зачем.** Физическая память даёт «дай 4 КиБ свободной памяти». Но процессы
в ОС работают с **виртуальной** памятью — каждому процессу кажется, что
он один в системе и у него есть все 256 ТиБ адресного пространства.
Это позволяет:
- Изолировать процессы друг от друга.
- Реализовать swap, copy-on-write, mmap.
- Защитить страницы (read-only, no-execute).

**Что в железе.** В разделе 1.5 мы уже видели 4-уровневые таблицы. Теперь
напишем **API**, чтобы ядро могло говорить «отобрази виртуальный адрес X
на физический адрес Y с такими-то правами».

**Файлы:**
- `ядро/src/арх/x86_64/страницы.rs` — низкоуровневое API.

Ключевые функции:

```rust
pub unsafe fn отобразить(
    виртуальный: u64,
    физический: u64,
    флаги: ФлагиСтраницы,  // PRESENT | WRITABLE | NO_EXECUTE
) -> Result<(), ОшибкаОтображения>;

pub unsafe fn снять_отображение(виртуальный: u64) -> Result<(), ОшибкаОтображения>;

pub fn перевести(виртуальный: u64) -> Option<u64>;  // walk по таблицам
```

**Алгоритм отображения:**

1. Берём 4 индекса из виртуального адреса (PML4, PDPT, PD, PT).
2. Спускаемся по таблицам. Если на каком-то уровне записи нет — выделяем
   через физический аллокатор и заполняем нулями.
3. На уровне PT записываем `физический | PRESENT | WRITABLE | (NO_EXECUTE)`.
4. **Сбрасываем TLB** — `invlpg [vaddr]`. Иначе CPU будет видеть старое
   отображение из своего кэша.

**Важно:** виртуальный адрес `0xFFFF_8000_0000_0000` зарезервирован для
ядра (старшая половина), пользовательские адреса — младшая половина
(0..0x7FFF_FFFF_FFFF). Это «**high-half kernel**» — стандартная схема,
где ядро отображено в каждом процессе.

> **Что мог бы пойти не так:**
> 1. *Мы наступили*: забыть `invlpg` → CPU работает по старому
>    отображению, новые страницы выглядят как «доступа нет».
> 2. Использовать non-canonical адрес → #GP.
> 3. Поставить `PRESENT | NO_EXECUTE` на страницу с кодом → #PF при
>    попытке выполнить инструкцию.

<a id="23-куча"></a>
### 2.3 Ядерная куча: динамическое выделение

**Зачем.** Физическая память даёт страницы по 4 КиБ. Виртуальная память
позволяет адресовать. Но нам нужно `Box::new(SomeStruct)` — выделение
произвольного размера (16 байт, 100 байт, 4 КиБ + 1 байт).

**Что в железе.** Никаких железных требований — это чисто алгоритм.
Стандартное решение — **free list** (список свободных блоков) или
**buddy allocator**.

**Что мы сделали.** Простой free-list:

1. Резервируем виртуальный диапазон для кучи (например, 2 МиБ).
2. Отображаем его в 512 физических страниц.
3. Поддерживаем связный список свободных блоков. Каждый блок начинается с
   заголовка `{ размер, следующий }`.

**Файлы:**
- `ядро/src/память/куча.rs` — реализация `АллокаторКучи`.
- `ядро/src/память/global.rs` — установка как `#[global_allocator]`.

```rust
#[global_allocator]
static ГЛОБАЛЬНЫЙ_АЛЛОКАТОР: ОбёрткаАллокатора = ОбёрткаАллокатора(...);

unsafe impl GlobalAlloc for ОбёрткаАллокатора {
    unsafe fn alloc(&self, layout: Layout) -> *mut u8 { /* free-list поиск */ }
    unsafe fn dealloc(&self, ptr: *mut u8, layout: Layout) { /* возврат в список */ }
}
```

После этого `alloc` крейт видит наш аллокатор и `Box`, `Vec`, `String`,
`BTreeMap` — всё работает.

**Самопроверка** (`главный.rs`):

```rust
let вектор: Vec<i32> = (0..16).collect();
let сумма_квадратов: i32 = вектор.iter().map(|x| x*x).sum();  // 1240
assert_eq!(сумма_квадратов, 1240);
```

> **Что мог бы пойти не так:**
> 1. Не выровнять адреса — `Box<u64>` хочет адрес, кратный 8.
> 2. Не вычитать заголовок при `dealloc` — потеряете байты или вернёте
>    мусор в free-list.
> 3. Не объединять соседние свободные блоки → фрагментация.

<a id="24-потоки"></a>
### 2.4 Потоки и переключение контекста

**Зачем.** Мы хотим запустить несколько задач параллельно (хотя CPU у
нас один). Решение — **многопоточность с переключением контекста**.

**Что в железе.** Состояние потока — это все его регистры:

- `rip` — где он остановился.
- `rsp`, `rbp` — стек.
- `rax`, `rbx`, ... — данные.
- `rflags` — флаги.

Чтобы переключиться с потока A на поток B:

1. Сохранить регистры A в его TCB (Thread Control Block).
2. Загрузить регистры B из его TCB.
3. Прыгнуть в `B.rip`.

В System V AMD64 ABI (наша) **callee-saved регистры** (`rbx`, `rbp`,
`r12-r15`) должны сохраняться вызываемой функцией. Если переключение
контекста — это просто вызов функции, нам надо сохранить только
callee-saved (плюс `rsp`).

**Файл:** `ядро/src/арх/x86_64/контекст.s`

```asm
.global _dub_switch_context
_dub_switch_context:
    ; rdi = старый_RSP_указатель (куда сохранить)
    ; rsi = новый_RSP (куда переключиться)
    
    ; Сохраняем callee-saved
    push rbp
    push rbx
    push r12
    push r13
    push r14
    push r15
    
    ; Записываем текущий RSP в *старый
    mov [rdi], rsp
    
    ; Переключаемся на новый стек
    mov rsp, rsi
    
    ; Восстанавливаем callee-saved нового потока
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbx
    pop rbp
    
    ret  ; возвращаемся туда, куда указывает [rsp] нового потока
```

**Создание нового потока** (`ядро/src/потоки/mod.rs`):

```rust
pub fn создать(стартовая_функция: fn()) -> ПотокИд {
    let стек = Vec::with_capacity(16 * 1024);  // 16 КиБ стек
    let верх_стека = стек.as_ptr() as u64 + 16384;
    
    // Кладём «начальный кадр» — как будто поток уже один раз был
    // переключён и сейчас должен вернуться в _dub_thread_trampoline,
    // который вызовет нашу стартовую_функция.
    
    let мой_рсп = подготовить_стек(верх_стека, стартовая_функция);
    
    ТКБ { rsp: мой_рсп, ид: следующий_ид(), стек }
}
```

Трамплин `_dub_thread_trampoline` — небольшая ASM-функция, которая:
1. Берёт указатель на `стартовая_функция` из стека.
2. Вызывает её.
3. Если функция вернулась — вызывает `сисв_завершить()`.

<a id="25-планировщик"></a>
### 2.5 Кооперативный планировщик

**Зачем.** Имея несколько потоков и `_dub_switch_context`, нам нужно
*решать*, на какой поток переключиться. Простейшая стратегия —
**кооперативный планировщик**: поток сам говорит «теперь моя очередь
закончилась» через `сисв_уступить()`.

**Файл:** `ядро/src/планировщик.rs` — FIFO-очередь готовых потоков.

```rust
pub fn уступить() {
    let следующий = ОЧЕРЕДЬ.lock().взять();
    let текущий = ТЕКУЩИЙ.lock();
    if let Some(следующий) = следующий {
        ОЧЕРЕДЬ.lock().положить(*текущий);
        unsafe { переключить_контекст(&текущий.rsp, следующий.rsp) };
    }
    // Если очередь пуста — продолжаем сами.
}
```

**Самопроверка:**

Создаём 3 потока, каждый делает 3 итерации:
```
поток 1: print "[ПОТОК 1]=N", уступить()
поток 2: print "[ПОТОК 2]=N", уступить()
поток 3: print "[ПОТОК 3]=N", уступить()
```

Ожидаем вывод: `[ПОТОК 1]=1 [ПОТОК 2]=2 [ПОТОК 3]=3 [ПОТОК 1]=4 [ПОТОК
2]=5 [ПОТОК 3]=6 [ПОТОК 1]=7 [ПОТОК 2]=8 [ПОТОК 3]=9`. Это и видно в
QEMU — переключение работает.

**Кооперативный** означает: если поток не вызывает `уступить()`, он будет
работать вечно и никто другой не получит CPU. **Вытесняющий** планировщик
(когда таймер сам прерывает поток) сделаем в следующем этапе после Этапа 4.

> **Что мог бы пойти не так:**
> 1. Не сохранять callee-saved → следующий поток получает мусор в `rbx`.
> 2. Не выровнять начальный RSP по 16 байт → `movaps`-инструкции в
>    SSE-коде падают с #GP.
> 3. Положить адрес функции на стек *после* callee-saved вместо *перед* —
>    `ret` прыгнет не туда.

<a id="26-ипк"></a>
### 2.6 ИПК: общение через порты

**Зачем.** В микроядре процессы общаются через сообщения. Нам нужен механизм
передачи данных от потока A к потоку B без shared memory.

**Что мы сделали.** **Порты** — каналы с очередью сообщений:

```rust
pub struct Порт {
    очередь: VecDeque<Сообщение>,
    мощность: usize,
    закрыт: bool,
}

pub struct Сообщение {
    отправитель: ПотокИд,
    значение: u64,
    данные: Vec<u8>,  // полезная нагрузка
}
```

**Системные вызовы** (`сисвызовы/mod.rs`):

- `сисв_создать_порт()` → возвращает хэндл (capability).
- `сисв_отправить(хэндл, сообщение)` → кладёт в очередь.
- `сисв_принять(хэндл) -> Сообщение` → блокирует, пока не появится сообщение.
- `сисв_закрыть(хэндл)` → закрывает порт.

**Самопроверка:**

```
Поток 1: создаёт порт, отправляет сообщения 10, 20, 30
Поток 2: принимает сообщения, суммирует — должно быть 60
```

В QEMU это и видно: `[OK]   ИПК: получено 3 сообщения, сумма значений
60 (10+20+30)`.

<a id="27-способности"></a>
### 2.7 Способности: безопасность через хэндлы

**Зачем.** Что мешает потоку B отправить сообщение в порт, который ему
не принадлежит? Если каждый знает идентификатор любого порта — это
небезопасно.

**Решение** — **способности** (capabilities). Каждый поток имеет свою
**таблицу хэндлов**. Хэндл — это **запись с правами**:

```rust
pub struct Способность {
    цель: ИдОбъекта,        // на какой объект (порт, поток, …)
    права: МаскаПрав,        // ОТПРАВКА | ПРИЁМ | ЗАКРЫТИЕ
}
```

**МаскаПрав** — битовая маска:
- `ОТПРАВКА` — можно делать `сисв_отправить`.
- `ПРИЁМ` — можно делать `сисв_принять`.
- `ЗАКРЫТИЕ` — можно делать `сисв_закрыть`.

**Создатель порта получает все права (мастер-способность).** Когда поток
A хочет дать другому потоку доступ к порту, он создаёт **обрезанную**
способность через `сисв_ограничить_способность(хэндл, маска)`. Например:

```rust
let мастер: u32 = сисв_создать_порт();  // все права
let только_отправка: u32 = сисв_ограничить_способность(мастер, ОТПРАВКА_БИТ);
// можно безопасно передать только_отправка другому потоку — он сможет
// отправлять сообщения, но не сможет читать их или закрыть порт.
```

**Самопроверка** проверяет:
1. Без ПРИЁМА — `сисв_принять` возвращает ошибку.
2. Полный путь отправка→приём — работает.
3. После закрытия порта — отправка возвращает ошибку.

> **Что мог бы пойти не так:**
> 1. Дать всем потокам общую таблицу хэндлов → один может «угадать»
>    хэндлы другого.
> 2. Не проверять права при `сисв_*` → способности нерабочие.

---

<a id="этап-3"></a>
## Этап 3 — Прерывания: реакция на железо

После Этапа 2 у нас есть микроядро, которое умеет создавать потоки и
обрабатывать ИПК. Но всё это — **пользовательская инициатива**: код сам
вызывает функции. А что если в систему пришёл байт от сетевой карты?
Или нажата клавиша? Кто-то должен **прервать** текущий поток и обработать.

<a id="31-gdt"></a>
### 3.1 GDT с TSS: фундамент защищённого режима

**Зачем.** В Этапе 1 мы создали минимальную GDT с тремя записями
(null, code, data). Для прерываний и для будущей поддержки кольца 3
(пользовательский режим) нужно больше.

**Что в железе.** Когда происходит прерывание из ring 3 (пользователь) в
ring 0 (ядро), процессор:

1. Смотрит в **TSS** (Task State Segment) — у TSS есть поле `RSP0` (стек
   ядра для ring 0).
2. Переключает SS:RSP на `(0, RSP0)`.
3. Кладёт на новый стек ring 3 SS, RSP, RFLAGS, CS, RIP.
4. Прыгает в обработчик прерывания.

Без TSS прерывание из ring 3 положит данные на стек **пользователя**, что
пользователь может прочитать или испортить.

**Кроме того:** некоторые исключения (особенно `#DF` — Double Fault)
могут случиться когда обычный стек разрушен. Для них в TSS есть
**IST** (Interrupt Stack Table) — массив из 7 альтернативных стеков. Можно
сказать «обработчик #DF используй IST1».

**Файл:** `ядро/src/арх/x86_64/гдт.rs`

```rust
#[repr(C, packed)]
struct ТссЗапись {
    зарезервировано1: u32,
    rsp0: u64, rsp1: u64, rsp2: u64,
    зарезервировано2: u64,
    ist1: u64, ist2: u64, ist3: u64, ist4: u64, ist5: u64, ist6: u64, ist7: u64,
    зарезервировано3: u64, зарезервировано4: u16,
    смещение_iomap: u16,
}

static mut ГДТ: [u64; 7] = [
    0,                                                 // 0: null
    /* код ядра    */ дескриптор(KERNEL_CODE),
    /* данные ядра */ дескриптор(KERNEL_DATA),
    /* код польз.  */ дескриптор(USER_CODE),
    /* данные польз*/ дескриптор(USER_DATA),
    /* TSS низ     */ tss_low,
    /* TSS верх    */ tss_high,
];
```

**Инициализация:**

```rust
pub unsafe fn инициализировать() {
    // Заполнить TSS адресом стека ядра + IST1
    TSS.rsp0 = СТЕК_ЯДРА.верх();
    TSS.ist1 = СТЕК_DF.верх();
    
    // Записать TSS-дескриптор в ГДТ[5..7]
    // ...
    
    // Загрузить GDT
    asm!("lgdt [{}]", in(reg) &ГДТ_УКАЗАТЕЛЬ);
    
    // Перезагрузить CS через дальний возврат
    asm!("push 0x08; lea rax, [rip + 1f]; push rax; retfq; 1:");
    
    // Перезагрузить data segments
    asm!("mov ax, 0x10; mov ds, ax; mov es, ax; mov ss, ax; ...");
    
    // Загрузить TSS
    asm!("ltr ax", in("ax") 0x28u16);  // селектор TSS
}
```

> **Что мог бы пойти не так:**
> *Мы наступили*: в `lea rax, [rip + 1f]` метка `1` — это битовая
> восьмёрка для inline-asm Rust («не используй цифры 0/1 как метки»).
> Пришлось переименовать в `2:`.

<a id="32-idt"></a>
### 3.2 IDT: таблица обработчиков прерываний

**Зачем.** Теперь надо сказать процессору, **куда прыгать** на каждое
прерывание. Это делается через **IDT** (Interrupt Descriptor Table).

**Что в железе.** IDT — массив из 256 записей по 16 байт. Каждая запись:

```
+0:   адрес обработчика, биты 0-15
+2:   селектор сегмента кода (обычно 0x08)
+4:   IST индекс (0..7) | reserved | type (0xE = int gate, 0xF = trap)
+6:   адрес обработчика, биты 16-31
+8:   адрес обработчика, биты 32-63
+12:  reserved
```

**Векторы 0..31** — зарезервированы CPU для исключений:
- 0: #DE — деление на ноль
- 1: #DB — отладочное прерывание (single-step)
- 6: #UD — недопустимая инструкция
- 8: #DF — двойная ошибка (использует IST1)
- 13: #GP — общая защитная ошибка (нарушение сегмента)
- 14: #PF — ошибка страницы (виртуальная память)
- ...

**Векторы 32..255** — программируемые. Мы используем 32..47 для IRQ
(8259 PIC).

**ASM-трамплины.** Когда срабатывает прерывание, CPU автоматически
кладёт на стек:

```
SS, RSP_USER, RFLAGS, CS, RIP   (если был переход с ring 3)
RFLAGS, CS, RIP                  (если был ring 0)
```

Для некоторых исключений CPU также кладёт **код ошибки**: #DF, #GP, #PF,
и т.д.

Прежде чем вызывать Rust-обработчик, надо **сохранить все 15 GP-регистров**
(чтобы при возврате ничего не пропало). Делаем это в ASM:

**Файл:** `ядро/src/арх/x86_64/идт.s`

```asm
.macro ТРАМПЛИН_ИСКЛЮЧЕНИЯ_БЕЗ_КОДА вектор
.global _dub_exc_\вектор
_dub_exc_\вектор:
    push 0          ; фиктивный код ошибки (для единообразия с ним)
    push \вектор    ; номер вектора
    push rax
    push rbx
    push rcx
    ; ... все 15 GP-регистров
    
    mov rdi, rsp    ; первый аргумент = указатель на КадрПрерывания
    cld
    call _dub_exception_handler
    
    pop r15
    ; ... все 15 регистров обратно
    add rsp, 16     ; пропустить вектор+код
    iretq           ; возврат — CPU сам восстановит RFLAGS, CS, RIP
.endm

ТРАМПЛИН_ИСКЛЮЧЕНИЯ_БЕЗ_КОДА 0   ; #DE
ТРАМПЛИН_ИСКЛЮЧЕНИЯ_БЕЗ_КОДА 1   ; #DB
; ...
ТРАМПЛИН_ИСКЛЮЧЕНИЯ_С_КОДОМ 8    ; #DF (с кодом ошибки)
; ...
```

**Rust-обработчик** (`ядро/src/арх/x86_64/идт.rs`):

```rust
const НАЗВАНИЕ_ИСКЛЮЧЕНИЯ: [&str; 32] = [
    "Деление на ноль",
    "Отладочное прерывание",
    "Немаскируемое прерывание",
    "Точка останова",
    "Переполнение",
    "Превышение границы",
    "Недопустимая инструкция",
    "Сопроцессор недоступен",
    "Двойная ошибка",
    /* ... */
    "Ошибка страницы",
    /* ... */
];

#[repr(C)]
pub struct КадрПрерывания {
    pub r15: u64, pub r14: u64, /* ... */ pub rax: u64,
    pub номер_вектора: u64,
    pub код_ошибки: u64,
    pub rip: u64, pub cs: u64, pub rflags: u64, pub rsp: u64, pub ss: u64,
}

#[no_mangle]
pub extern "C" fn _dub_exception_handler(кадр: &mut КадрПрерывания) {
    let имя = НАЗВАНИЕ_ИСКЛЮЧЕНИЯ[кадр.номер_вектора as usize];
    println!("‼ ИСКЛЮЧЕНИЕ: {}", имя);
    println!("  RIP = {:016X}", кадр.rip);
    println!("  CS  = {:016X}", кадр.cs);
    println!("  Код = {:016X}", кадр.код_ошибки);
    if кадр.номер_вектора == 14 {
        // #PF — печатаем CR2 (адрес, на котором упало)
        let cr2: u64;
        unsafe { asm!("mov {}, cr2", out(reg) cr2) };
        println!("  CR2 = {:016X}", cr2);
    }
    // Остановка ядра.
    crate::арх::халт_навсегда();
}
```

> **Что мог бы пойти не так:**
> 1. *Мы наступили*: записи `extern "C"` не разрешают кириллические
>    имена → пришлось всем `_исключение_N` переименовать в `_dub_exc_N`,
>    а Rust-обёртки оставить как русские.
> 2. Забыть `cld` перед вызовом C-функции → если флаг направления был
>    установлен, `rep movsb` будет копировать в обратную сторону.
> 3. `iretq` без `add rsp, 16` → стек поползёт.

<a id="33-pic"></a>
### 3.3 8259 PIC: контроллер прерываний из 1976

**Зачем.** Векторы 0..31 — зарезервированы CPU для исключений. Внешние
прерывания (клавиатура, диск) приходят от **контроллера прерываний** —
8259 PIC. По умолчанию BIOS настраивает PIC так, что его прерывания
приходят на векторы 8..15, **которые пересекаются с исключениями CPU**!

Поэтому первое, что мы делаем — **перепрограммируем PIC**, чтобы его
прерывания приходили на 32..47.

**Что в железе.** 8259 PIC — пара чипов (master + slave) подключённых к
портам:
- Master: команды 0x20, данные 0x21.
- Slave: команды 0xA0, данные 0xA1.

Для перепрограммирования посылаем последовательность ICW1..ICW4:

```
Master: 0x20 ← 0x11    (ICW1: инициализация)
Slave:  0xA0 ← 0x11
Master: 0x21 ← 0x20    (ICW2: вектор смещения = 0x20)
Slave:  0xA1 ← 0x28    (ICW2: вектор смещения = 0x28)
Master: 0x21 ← 0x04    (ICW3: slave на IRQ2)
Slave:  0xA1 ← 0x02    (ICW3: каскадный)
Master: 0x21 ← 0x01    (ICW4: 8086 режим)
Slave:  0xA1 ← 0x01
Master: 0x21 ← 0xFF    (маски: всё запрещено по умолчанию)
Slave:  0xA1 ← 0xFF
```

**Файл:** `ядро/src/арх/x86_64/pic.rs`

```rust
pub unsafe fn перенастроить() {
    /* отправляем ICW1..ICW4 как описано выше */
}

pub unsafe fn снять_маску_ирк(ирк: u8) {
    let (порт, бит) = if ирк < 8 { (PIC1_ДАННЫЕ, ирк) } else { (PIC2_ДАННЫЕ, ирк - 8) };
    let маска = ввести_порт(порт);
    вывести_порт(порт, маска & !(1 << бит));  // обнуляем бит = разрешаем IRQ
}

pub fn подтвердить(вектор: u8) {
    // EOI (End Of Interrupt) — должен быть отправлен после обработки
    if вектор >= 0x28 {
        вывести_порт(PIC2_КОМАНДЫ, 0x20);  // если IRQ от slave
    }
    вывести_порт(PIC1_КОМАНДЫ, 0x20);
}
```

**Карта IRQ:**
- IRQ0 = таймер (PIT)
- IRQ1 = клавиатура (PS/2)
- IRQ2 = каскад (slave)
- IRQ3-7 = COM2/4, COM1/3, LPT2, дисковод, LPT1
- IRQ8 = RTC (часы реального времени)
- IRQ12 = мышь PS/2
- IRQ14 = первичный ATA (диск)
- IRQ15 = вторичный ATA

> **Что мог бы пойти не так:**
> 1. Забыть отправить EOI → следующее IRQ той же линии не придёт.
> 2. Отправить EOI только в master, когда IRQ был от slave → slave
>    «думает», что IRQ ещё не обработан.

<a id="34-pit"></a>
### 3.4 PIT: таймер 100 раз в секунду

**Зачем.** Чтобы запустить вытесняющий планировщик, нужен **периодический
сигнал**. PIT (Programmable Interval Timer) — стандартный таймер с 1981
года, выдаёт IRQ0.

**Что в железе.** PIT работает на тактовой частоте 1.193182 МГц.
Делитель — 16-битное число. Канал 0 в режиме 3 (square wave) выдаёт
прерывание каждые `делитель` тактов:

```
делитель = 1193182 / желаемая_частота
```

Для 100 Гц делитель = 11931 (`1193182 / 100`).

**Файл:** `ядро/src/арх/x86_64/pit.rs`

```rust
const ВНУТРЕННЯЯ_ЧАСТОТА: u32 = 1_193_182;

pub unsafe fn инициализировать(гц: u32) {
    let делитель = (ВНУТРЕННЯЯ_ЧАСТОТА / гц) as u16;
    
    // Команда: канал 0, lobyte/hibyte, режим 3, бинарный счётчик
    вывести_порт(0x43, 0x36);
    вывести_порт(0x40, (делитель & 0xFF) as u8);
    вывести_порт(0x40, (делитель >> 8) as u8);
}
```

**Самопроверка** в Этапе 3:

```rust
let начало = ТИКИ.load(Relaxed);
задержка_тиков(25);  // hlt пока ТИКИ не вырастет на 25 (≈250 мс)
let прошло = ТИКИ.load(Relaxed) - начало;
assert!(прошло >= 20);  // допускаем погрешность
```

В QEMU это работает: `[OK]   Таймер: 25 тиков (~250 мс), IRQ0 поднят 26 раз`.

<a id="35-каркас"></a>
### 3.5 Каркас драйверов

**Зачем.** Чтобы клавиатуру, мышь, диск можно было добавлять единообразно,
без переписывания ядра, нужен **общий интерфейс**.

**Файл:** `ядро/src/драйверы/mod.rs`

```rust
pub trait Драйвер: Send + Sync {
    fn имя(&self) -> &'static str;
    fn инициализировать(&mut self) -> Result<(), &'static str>;
    fn обработать_прерывание(&mut self) {}
    fn завершить(&mut self) {}
}

pub struct Реестр {
    записи: Vec<Запись>,
}

impl Реестр {
    pub fn зарегистрировать(&mut self, драйвер: Box<dyn Драйвер>) -> Result<(), &'static str>;
    pub fn найти_изм(&mut self, имя: &str) -> Option<&mut dyn Драйвер>;
}

static РЕЕСТР: Mutex<Реестр> = Mutex::new(Реестр::пустой());
```

**Маршрутизатор IRQ** (`ядро/src/прерывания/маршрутизатор.rs`):

```rust
static ПРИВЯЗКИ: Mutex<[Option<&'static str>; 16]> = Mutex::new([None; 16]);

pub fn привязать_ирк(ирк: u8, имя_драйвера: &'static str) -> Result<(), &'static str>;

pub fn обработать_ирк(вектор: u8, кадр: &mut КадрПрерывания) {
    let ирк = вектор - 0x20;
    if ирк == 0 { /* таймер */ таймер::тик(); }
    
    let имя = ПРИВЯЗКИ.lock()[ирк as usize];
    if let Some(имя) = имя {
        if let Some(mut реестр) = РЕЕСТР.try_lock() {
            if let Some(драйвер) = реестр.найти_изм(имя) {
                драйвер.обработать_прерывание();
            }
        }
    }
}
```

**Использование** (для нового драйвера):

```rust
struct МойДрайвер;

impl Драйвер for МойДрайвер {
    fn имя(&self) -> &'static str { "мой-драйвер" }
    fn инициализировать(&mut self) -> Result<(), &'static str> { Ok(()) }
    fn обработать_прерывание(&mut self) {
        // читаем регистры устройства, обрабатываем
    }
}

драйверы::зарегистрировать(Box::new(МойДрайвер))?;
прерывания::маршрутизатор::привязать_ирк(7, "мой-драйвер")?;
pic::снять_маску_ирк(7);
```

И всё. **Это — главное достижение Этапа 3**: теперь любой драйвер можно
добавить в десять строк, без изменений в ядре.

> **Тонкость:** маршрутизатор использует `try_lock`, а не `lock`. Это
> чтобы прерывание никогда не зависло в ожидании реестра, если ядерный
> код держит его. Потеряем тик — переживём; зависнем — нет.

---

<a id="этап-4"></a>
## Этап 4 — Драйверы: клавиатура и отладочное окно

После Этапа 3 у нас есть каркас. Этап 4 — построить на нём **первый
настоящий драйвер** и **отладочное окно** для просмотра логов в реальном
времени.

<a id="41-журнал"></a>
### 4.1 Журнал логов

**Зачем.** При загрузке ядра проходят десятки `println!`. Они уходят в
VGA + COM1 и пропадают. Нам нужно их **сохранить** в памяти, чтобы потом
посмотреть в отладочном окне.

**Что мы сделали.** **Кольцевой буфер** на 32 КиБ.

**Файл:** `ядро/src/журнал.rs`

```rust
const РАЗМЕР_БУФЕРА: usize = 32 * 1024;

pub struct КольцевойЖурнал {
    данные: [u8; РАЗМЕР_БУФЕРА],
    позиция: usize,         // куда писать дальше
    всего_записано: usize,  // для определения, заполнен ли буфер
}

impl КольцевойЖурнал {
    pub fn записать(&mut self, текст: &str) {
        for &байт in текст.as_bytes() {
            self.данные[self.позиция % РАЗМЕР_БУФЕРА] = байт;
            self.позиция = self.позиция.wrapping_add(1);
            self.всего_записано = self.всего_записано.saturating_add(1);
        }
    }
    
    pub fn скопировать_хвост(&self, выход: &mut [u8]) -> usize { /* ... */ }
}

static ЖУРНАЛ: Mutex<КольцевойЖурнал> = Mutex::new(КольцевойЖурнал::новый());
```

**Подключение к печати.** В `печать.rs` мы дополняем `_печать`, чтобы
параллельно с VGA + COM1 он писал и в журнал:

```rust
pub fn _печать(аргументы: fmt::Arguments) {
    crate::арх::с_носителями_печати(|носитель| { /* VGA + COM1 */ });
    let _ = ЖурналныйНоситель.write_fmt(аргументы);  // ← новое
}

struct ЖурналныйНоситель;
impl Write for ЖурналныйНоситель {
    fn write_str(&mut self, s: &str) -> fmt::Result {
        crate::журнал::записать(s);
        Ok(())
    }
}
```

После этого **каждый** `println!` автоматически попадает и в журнал. Без
изменений в самих макросах.

**Тонкость:** запись в журнал использует `try_lock` — если журнал уже
заблокирован (например, отладочное окно его читает), мы просто
пропустим запись. Это безопасно, потому что журнал — диагностический.

<a id="42-клавиатура"></a>
### 4.2 PS/2-клавиатура

**Зачем.** Чтобы пользователь мог нажать F12 и открыть отладочное окно,
нам нужно **читать клавиатуру**. На QEMU и большинстве реальных ноутбуков
до 2015 года клавиатура — PS/2 (или USB, эмулирующий PS/2 через
**Legacy USB Support** в BIOS).

**Что в железе.** PS/2 контроллер 8042 имеет:
- Порт `0x60` — данные (сканкоды от клавиатуры).
- Порт `0x64` — статус/команды.
- IRQ1 — каждое нажатие/отпускание.

Когда клавиша нажата, контроллер кладёт **сканкод** в порт 0x60 и
поднимает IRQ1. Мы должны быстро прочитать байт, иначе следующий
потеряется.

**Сканкоды Set 1** — стандарт PC AT (1984). Каждая клавиша имеет свой
**make code** (нажатие) и **break code** (отпускание = make code | 0x80).

```
0x1E = A make,  0x9E = A break
0x10 = Q make,  0x90 = Q break
0x39 = пробел
0x1C = Enter
0x01 = Esc
0x58 = F12  ← наша горячая клавиша
```

**Расширенные сканкоды** начинаются с **0xE0**: стрелки, Home/End/PgUp/
PgDn, правый Ctrl/Alt, и т.п.

**Файл:** `ядро/src/драйверы/клавиатура.rs`

```rust
pub struct ДрайверКлавиатуры;

impl Драйвер for ДрайверКлавиатуры {
    fn имя(&self) -> &'static str { "клавиатура-ps2" }
    
    fn инициализировать(&mut self) -> Result<(), &'static str> {
        // Очистить буфер контроллера
        for _ in 0..16 {
            if (вв8(0x64) & 1) == 0 { break; }
            let _ = вв8(0x60);
        }
        Ok(())
    }
    
    fn обработать_прерывание(&mut self) {
        let байт = вв8(0x60);
        let mut с = СОСТОЯНИЕ.lock();
        
        if байт == 0xE0 { с.расширенный = true; return; }
        
        let нажата = (байт & 0x80) == 0;
        let код = байт & 0x7F;
        
        // Обновляем модификаторы (Shift/Ctrl/Alt)
        match (с.расширенный, код) {
            (false, 0x2A) | (false, 0x36) => { с.shift_нажат = нажата; ... }
            (_, 0x1D) => { с.ctrl_нажат = нажата; ... }
            (_, 0x38) => { с.alt_нажат = нажата; ... }
            _ => {}
        }
        
        if !нажата { return; }  // отпускания не интересуют
        
        let клавиша = расшифровать_сканкод(код, с.расширенный, с.shift_нажат);
        
        // Горячие клавиши — атомные флаги
        match клавиша {
            Клавиша::F(12) => ПРИШЛА_F12.store(true, Relaxed),
            Клавиша::PgUp => ПРИШЛА_PGUP.store(true, Relaxed),
            Клавиша::PgDn => ПРИШЛА_PGDN.store(true, Relaxed),
            Клавиша::Esc => ПРИШЛА_ESC.store(true, Relaxed),
            _ => {}
        }
        
        с.положить(клавиша);  // в FIFO-очередь
    }
}
```

**Регистрация:**

```rust
драйверы::зарегистрировать(Box::new(ДрайверКлавиатуры::новый()))?;
прерывания::маршрутизатор::привязать_ирк(1, "клавиатура-ps2")?;
pic::снять_маску_ирк(1);
```

И всё. С этого момента каждое нажатие приходит в наш драйвер.

> **Что мог бы пойти не так:**
> 1. Не прочитать порт 0x60 → следующее IRQ1 не придёт (контроллер
>    «думает», что байт ещё не забрали).
> 2. Не отправить EOI в PIC → IRQ1 больше не сработает.
> 3. Использовать `lock()` в IRQ-обработчике → если очередь читается
>    другим потоком и тот «зацепился» — IRQ зависнет.

<a id="43-окно"></a>
### 4.3 Отладочное окно по F12

**Зачем.** Объединяем журнал + клавиатуру в **визуализацию логов**.
Нажал F12 — увидел всё, что произошло с момента загрузки.

**Что мы сделали** (`ядро/src/отладка/окно.rs`):

1. В главном цикле ядра вызываем `обновить()` каждый раз после `hlt`.
2. `обновить()` читает горячие флаги клавиатуры:
   - F12 → переключить состояние окна (открыто/закрыто).
   - PgUp / PgDn → прокрутить.
   - Esc → закрыть.
3. При открытии:
   - Сохранить снимок текущего экрана (`СНИМОК: [u16; 80*25]`).
   - Отключить запись в журнал (чтобы не зациклиться через свои же `println!`).
   - Отрисовать заголовок (красная полоса), журнал, рамку, статус.
4. При закрытии:
   - Восстановить снимок.
   - Включить запись в журнал.

**Декодирование UTF-8.** В журнале `println!("Привет")` хранится как
12 байт UTF-8 (`П` = два байта). При отрисовке мы декодируем по байтам
до символов и переводим через `кодировка::в_дубкод(символ) -> байт_VGA`:

```rust
let байт = срез[поз];
let длина_посл = if байт < 0x80 { 1 }
    else if байт & 0b1110_0000 == 0b1100_0000 { 2 }
    else if байт & 0b1111_0000 == 0b1110_0000 { 3 }
    else { 4 };
let символ = core::str::from_utf8(&срез[поз..поз+длина_посл])
    .ok()
    .and_then(|с| с.chars().next())
    .unwrap_or('?');
ячейка(строка_окна, столбец, кодировка::в_дубкод(символ), АТР_ТЕКСТА);
поз += длина_посл;
столбец += 1;
```

**Самопроверка** в QEMU прошла:
- F12 → красный заголовок появился.
- PgUp PgUp → прокрутилось к более ранним строкам, статус показал
  «смещение от хвоста: 10».
- F12 ещё раз → экран байт-в-байт восстановился.

> **Что мог бы пойти не так:**
> 1. Не отключить запись в журнал при перерисовке окна → если окно
>    что-то печатает (для отладки), это попадает обратно в журнал и
>    смещает содержимое.
> 2. Не сохранить снимок → после закрытия окна экран остаётся пустым.
> 3. Lock на журнал в IRQ → можно зависнуть. Решение: в IRQ читаем
>    *снимок* (одноразовый `скопировать_хвост` под `try_lock`).

---

<a id="что-дальше"></a>
## Что дальше

После Этапа 4 у нас работает:

```
[ Загрузчик MBR + Stage2 ]                  ← Этап 1
   ↓
[ Long mode, GDT с TSS, IDT, PIC, PIT ]     ← Этап 1+3
   ↓
[ Память: физ+вирт, куча, потоки, ИПК ]     ← Этап 2
   ↓
[ Каркас драйверов ]                        ← Этап 3
   ↓
[ Клавиатура, журнал, F12-окно ]            ← Этап 4
```

Что **не** сделано (по приоритетам):

1. **Вытесняющий планировщик.** Сейчас потоки переключаются только через
   `сисв_уступить()`. В таймере мы получаем IRQ0, но *не вызываем*
   `уступить()` оттуда — для этого нужно сохранять **полный** регистровый
   контекст в IDT-кадре и переключать `RSP` на стек следующего потока
   перед `iretq`. Это переделка `вход.s` потоков — отдельный этап.

2. **Запуск ring 3 (пользовательский режим).** TSS уже готов, но мы не
   создаём пользовательские процессы. Нужен:
   - `iretq` с пользовательскими CS/SS.
   - syscall instruction (или `int 0x80` в legacy).
   - Изоляция адресных пространств.

3. **ATA/AHCI диск + ДубФС.** Сделаем после ring 3 — дисковому драйверу
   удобнее работать как пользовательскому процессу.

4. **Сеть.** RTL8139 или Intel E1000 + наш собственный TCP/IP. Это уже
   серьёзно, ждёт после ФС.

5. **Графика.** VBE/Bochs framebuffer. После графики можно сделать
   оконную систему «ДубаОкна» и перенести отладочное окно туда.

6. **Реальное железо.** Asus X552E (Intel Celeron N2840) — посмотреть
   ACPI, PCIe, USB. Это совсем другая ветка работы.

7. **Самохост — компилятор «Дубины» работает на ДубинаОС.** Сейчас
   компилятор `язык_дубина/компилятор` собирается на хосте. Чтобы
   запускать на ДубинаОС, нужен полноценный POSIX-совместимый IO-слой
   (хотя бы файлы и stdout).

Каждый из этих пунктов — отдельный этап в [`ДОРОЖНАЯ_КАРТА.md`](./ДОРОЖНАЯ_КАРТА.md).

---

## Полезные ссылки

- [OSDev Wiki](https://wiki.osdev.org/) — самый полный справочник по
  программированию x86 (и других).
- [Intel SDM](https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html) —
  официальная документация Intel (тысячи страниц).
- [AMD APM](https://www.amd.com/en/support/tech-docs) — AMD Architecture
  Programmer's Manual; иногда понятнее Intel.
- [Phil Opp's blog](https://os.phil-opp.com/) — пошаговое руководство по
  написанию микроядра на Rust (с другими решениями, чем у нас).
- [Brendan's tutorial](https://wiki.osdev.org/Brendan%27s_OS_Development_Tutorial) —
  классика на C.

---

*ДубинаОС, GPL-3.0-or-later, разработка идёт. Этот документ — живой,
будет дополняться по мере появления новых этапов.*
