# ДубинаОС — пример с динамическим числом и циклом.
# Демонстрирует тип `Число`, который автоматически расширяется до нужной разрядности.
# Лицензия: GPL-3.0-or-later

использовать стандарт.вывод

главная()():
    Число счётчик = 0       # 1 бит
    пока счётчик < 1000:    # внутри цикла Число вырастет до 10 бит
        вывод.печатать(счётчик)
        счётчик = счётчик + 1
    ;
;
