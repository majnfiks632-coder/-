# Спецификация языка «Дубина»

Версия 0.1.0 — черновик.

## 1. Философия

«Дубина» — это безопасный язык системного и прикладного программирования. Главные принципы:

1. **На нём нельзя написать вирус.** Программа без явно выданных capability может только вычислять. Никакого ambient authority, никаких сырых указателей, никакой рефлексии, никакого `eval`, никакого inline asm, никакого FFI.
2. **Память безопасна по построению.** Владение и заимствование (как в Rust). Без `null`, без use-after-free, без double-free, без data races, без переполнений буфера.
3. **Ошибки не исчезают.** Каждое потенциально неудачное действие возвращает `Результат<З,О>`. Каждое возможно-отсутствующее значение — `Опция<Т>`. Игнорировать их нельзя.
4. **Пишется по-русски.** Ключевые слова, типы, имена в стандартной библиотеке — на русском. UTF-8 идентификаторы.
5. **Память по умолчанию минимальна.** Тип `Число` динамически растёт по степеням двойки.

## 2. Лексика

### 2.1. Кодировка
Исходный код — UTF-8. Идентификаторы могут содержать любые буквы Юникода (категории `L*`) и цифры (`Nd`); первый символ — буква.

### 2.2. Комментарии
```
# до конца строки
```

### 2.3. Литералы
| Литерал | Пример |
|---|---|
| Целое (Число) | `0`, `42`, `1_000_000`, `0x1F`, `0b1010`, `0o755` |
| Десятичное | `1.5`, `1e10`, `2.71_8` |
| Дробь | `1/3`, `7/22` |
| Строка | `"Привет"` (с интерполяцией: `"Здравствуй, ${имя}!"`) |
| Логическое | `правда`, `да`, `ложь`, `нет` |
| Пусто | `пусто` (значение единичного типа) |

### 2.4. Ключевые слова
```
Класс  функция  пусть  изм  константа
Если   иначе    выбор  совпадение
пока   цикл     для    в    прервать  продолжить  вернуть
использовать    модуль публичная  приватная
структура       перечисление     черта   реализация
сам    новый    как    где
правда да       ложь   нет     пусто
и      или      не     ?        ??
асинх  ждать    пуск
```

### 2.5. Операторы
Арифметика: `+ - * / % **` (степень).  
Сравнение: `== != < <= > >=`.  
Логические: `и или не`.  
Битовые (только для фиксированных типов): `&| ^ ~ << >>`.  
Присваивание: `= += -= *= /= %=`.  
Доступ: `.` (поле/метод), `?` (распаковка `Опция`/`Результат`), `??` (значение по умолчанию).

## 3. Блоки

Открывающий маркер — двоеточие `:` в конце строки. Закрывающий — точка с запятой `;` на отдельной строке.

```
Если х > 0:
    консоль.печать("положительное")
;
```

Отступы синтаксически безразличны, но рекомендованы для читаемости (4 пробела).

## 4. Переменные

Объявление **необязательно**: первое присваивание создаёт переменную (как в Python). Тип выводится автоматически.

```
имя = "Мир"
возраст = 25
```

Можно объявить **явно** с типом перед именем:

```
Строка имя = "Мир"
Число возраст = 25
Десятичное температура = 36.6
Дробь шанс = 1/3
```

Опционально через ключевые слова:

```
пусть имя = "Мир"          # неизменяемая (по умолчанию)
изм счётчик = 0            # изменяемая
константа ПИ = 3.141_592   # компилируемая константа
```

## 5. Типы

### 5.1. Встроенные

| Тип | Описание |
|---|---|
| `Число` | **Динамическое** целое: занимает минимум бит, расширяется по степеням двойки автоматически |
| `ц8 ц16 ц32 ц64 ц128` | Знаковые целые фиксированной ширины |
| `н1 н2 н4 н8 н16 н32 н64 н128` | Натуральные (беззнаковые) фиксированной ширины |
| `Десятичное` | 64-битное вещественное (IEEE 754 double) |
| `вщ32 вщ64` | Вещественные явной ширины |
| `Дробь` | Точная рациональная (числитель/знаменатель) |
| `Лог` | Логический (`правда`/`ложь`) |
| `Сим` | Символ Юникода (UTF-32) |
| `Строка` | UTF-8, неизменяемая |
| `Стройка` | UTF-8, изменяемая (mutable string builder) |
| `Список<Т>` | Динамический массив |
| `Срез<Т>` | Невладеющая ссылка на отрезок данных |
| `Карта<К,З>` | Ассоциативный массив |
| `Множество<Т>` | Множество |
| `Опция<Т>` | `Некоторое(Т)` или `Ничего` |
| `Результат<З,О>` | `Хорошо(З)` или `Плохо(О)` |
| `пусто` | Единичный тип (`()`) |

### 5.2. Динамическое `Число` — детально

В памяти переменная типа `Число` занимает:
```
[ 6 бит заголовка | N бит значения ]
```
где `N = 2^k` — количество бит, нужное для хранения текущего значения, округлённое до степени двойки.

| Значение | k | Биты значения | Всего бит |
|---|---|---|---|
| `0` | 0 | 1 | 7 |
| `4` | 2 | 4 | 10 |
| `15` | 4 | 16 | 22 |
| `1000` | 4 | 16 | 22 |
| `100000` | 5 | 32 | 38 |
| `10^18` | 6 | 64 | 70 |

При переполнении — автоматическое расширение, при необходимости — переход на heap-allocated bigint (рост сверх 64-х бит).

В **массивах** и **полях структур** числа упакованы вплотную, без выравнивания, с общим заголовком на блок.

При компиляции, если по анализу потока видно, что значение всегда укладывается в фиксированную ширину, компилятор автоматически использует `ц8`/`ц16`/`ц32`/`ц64` без заголовка.

Программист может зафиксировать ширину явно:
```
ц32 счётчик = 0
```

### 5.3. Структуры
```
структура Точка:
    Десятичное х
    Десятичное у
;
```

### 5.4. Перечисления (с данными)
```
перечисление Фигура:
    Круг(Десятичное радиус)
    Прямоугольник(Десятичное ширина, Десятичное высота)
    Треугольник(Десятичное a, Десятичное b, Десятичное c)
;
```

### 5.5. Черты (traits/interfaces)
```
черта Площадь:
    площадь()(результат: Десятичное)
;

реализация Площадь для Фигура:
    площадь()(результат: Десятичное):
        совпадение сам:
            Круг(р): результат = 3.141592 * р * р;
            Прямоугольник(ш, в): результат = ш * в;
            Треугольник(a, b, c):
                пусть s = (a + b + c) / 2.0
                результат = (s * (s-a) * (s-b) * (s-c)).корень()
            ;
        ;
    ;
;
```

## 6. Функции и методы

Объявление функции:
```
имя(параметры)(возвращаемые):
    тело
;
```

- Первая пара скобок — параметры (с опциональными типами).
- Вторая пара — возвращаемые значения (могут быть несколько). Если функция ничего не возвращает, оставляются пустыми `()`.
- В теле возвращаемые переменные доступны как локальные. По завершении тела они автоматически возвращаются. Можно вернуть досрочно через `вернуть знач1, знач2`.

```
сумма_и_разность(а, б)(сумма, разность):
    сумма = а + б
    разность = а - б
;

# Использование с распаковкой:
(с, р) = сумма_и_разность(10, 3)
```

Функция без возврата:
```
поприветствовать(имя)():
    консоль.печать("Привет, " + имя + "!")
;
```

Функции **высшего порядка** разрешены (функция как значение):
```
применить(ф, х)(результат):
    результат = ф(х)
;
```

## 7. Классы

```
Класс:Точка:
    Десятичное х
    Десятичное у
    
    # Конструктор
    новый(х, у)(сам):
        сам.х = х
        сам.у = у
    ;
    
    расстояние_до(другая)(результат: Десятичное):
        пусть дх = сам.х - другая.х
        пусть ду = сам.у - другая.у
        результат = (дх*дх + ду*ду).корень()
    ;
;

# Использование
п1 = Точка.новый(0.0, 0.0)
п2 = Точка.новый(3.0, 4.0)
консоль.печать(п1.расстояние_до(п2))   # 5.0
```

Поля по умолчанию приватные. Чтобы сделать публичными — пометка `публичная`.

## 8. Управление

### 8.1. Условия
```
Если x > 0:
    консоль.печать("плюс")
иначе Если x < 0:
    консоль.печать("минус")
иначе:
    консоль.печать("ноль")
;
```

### 8.2. Совпадения (pattern matching)
```
совпадение фигура:
    Круг(р): площадь = 3.14 * р * р;
    Прямоугольник(ш, в): площадь = ш * в;
;
```
Обязательно покрытие всех вариантов.

### 8.3. Циклы
```
пока счётчик < 10:
    счётчик = счётчик + 1
;

цикл i в 0..100:
    консоль.печать(i)
;

цикл элемент в список:
    обработать(элемент)
;

цикл:                     # бесконечный
    Если готово: прервать;
;
```

## 9. Безопасность памяти и доступа

### 9.1. Владение
Каждое значение имеет одного владельца. При присваивании или передаче в функцию — перемещение (move). Старая переменная становится недействительной.

```
а = "Мир"
б = а              # перемещение: а теперь невалидна
консоль.печать(а)  # ошибка компиляции
```

### 9.2. Заимствование
Чтение и запись через ссылки (компилятор отслеживает автоматически — обычно явно `&` писать не надо).

```
имя = "Мир"
поприветствовать(имя)     # компилятор заимствует, не перемещает
консоль.печать(имя)       # всё ещё валидно
```

### 9.3. Опция и Результат — обязательные
```
функция_деления(а, б)(результат: Результат<Десятичное, Ошибка>):
    Если б == 0.0:
        результат = Плохо(Ошибка.новый("деление на ноль"))
    иначе:
        результат = Хорошо(а / б)
    ;
;

# Использование с оператором ?
вычисление()(итог: Результат<Десятичное, Ошибка>):
    пусть x = функция_деления(10.0, 2.0)?    # ? распаковывает или возвращает ошибку
    итог = Хорошо(x + 1.0)
;
```

### 9.4. Capability-модель
Программа НЕ имеет доступа к файлам, сети, экрану, времени, случайным числам, и т.д. сама по себе. Все эти возможности приходят как параметры в функцию `главная`:

```
главная(консоль, файлы, время)():
    консоль.печать("сейчас " + время.текущая_дата())
;
```

Какие capability программа получит — определяется её **манифестом** (`приложение.дуб.манифест`), который проверяется службой разрешений при запуске. Без записи в манифесте capability не передаётся.

## 10. Стандартная библиотека (выборочно)

```
использовать стандарт.вывод.{Консоль}
использовать стандарт.файлы.{ХранилищеФайлов, Режим, Файл}
использовать стандарт.коллекции.{Список, Карта, Множество}
использовать стандарт.строки.{Строка, Стройка}
использовать стандарт.математика.{мин, макс, абс, корень, ПИ}
использовать стандарт.время.{Часы, Длительность, Момент}
использовать стандарт.криптография.{BLAKE3, AES_XTS}
использовать стандарт.сеть.{TCP, UDP, IPv4, IPv6}
```

## 11. Запреты

В пользовательском коде **отсутствуют**:
- Сырые указатели (`*const T`, `*mut T`)
- Inline assembly
- Внешние функции (FFI)
- `unsafe`-блоки (могут быть только в стандартной библиотеке, в специальных «доверенных» модулях, проверенных вручную)
- Рефлексия и `eval`
- Динамическая загрузка кода
- Самомодификация
- Доступ к памяти других процессов

## 12. Компиляция

Цепочка:
1. **Лексер** → токены
2. **Парсер** → AST
3. **Резолвер имён** → разрешение всех символов
4. **Проверка типов** + вывод типов
5. **Проверка заимствований** (borrow checker)
6. **Проверка манифеста** (программа не использует ничего, чего нет в манифесте)
7. **Промежуточное представление (ПП)** — нормализованное
8. **Оптимизации** (включая специализацию динамических `Число`)
9. **Кодоген** — нативный код для x86_64 / aarch64
10. **Линковка** в формат `.дубп` (исполняемый файл «Дубины»)

Файлы `.дубп` содержат:
- Заголовок с манифестом capability
- Подпись службы разрешений
- Метаданные для отладчика
- Машинный код

## 13. Пример программы целиком

```
# приветствие.дуб
использовать стандарт.вывод.Консоль

приветствовать(имя)(сообщение: Строка):
    сообщение = "Привет, " + имя + "!"
;

главная(консоль: Консоль)():
    Список<Строка> имена = ["Мир", "Девин", "Лиза"]
    цикл имя в имена:
        пусть с = приветствовать(имя)
        консоль.печать(с)
    ;
;
```

Манифест:
```
# приветствие.дуб.манифест
имя = "Приветствие"
версия = "1.0.0"
capability:
    - стандарт.вывод.Консоль
```

---

Эта спецификация — живой документ. Уточнения и расширения в процессе разработки компилятора.
